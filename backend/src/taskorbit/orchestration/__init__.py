"""Orchestration engine.

Routes the conversation to the right task agent, plans multi-step
workflows, asks for user confirmation before execution, and dispatches
tool calls only after approval.

Implementation is owned by downstream feature tickets.
"""
