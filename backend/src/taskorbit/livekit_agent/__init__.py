"""LiveKit agent worker.

Joins LiveKit rooms, runs the voice pipeline (STT → LLM → TTS),
and handles interruption / turn detection / session control.

Implementation is owned by downstream feature tickets — this package
is a placeholder created by the dev-environment setup.
"""
