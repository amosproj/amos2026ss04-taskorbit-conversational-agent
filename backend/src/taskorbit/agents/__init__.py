"""Task-specific agents (technical, sales, …).

Each agent encapsulates a focused conversational role. The orchestration
layer hands off between them as the conversation progresses.

Implementation is owned by downstream feature tickets.
"""
