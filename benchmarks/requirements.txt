# Benchmark Requirements

# Core dependencies
pyyaml>=6.0
pydantic>=2.0
httpx>=0.28.0

# Optional: metrics export (for --upload-metrics)
# opentelemetry-exporter-prometheus>=0.40b0
# opentelemetry-exporter-otlp>=0.40b0

# Optional: system metrics collection
psutil>=5.9.0

# Optional: plotting (for future visualization)
# matplotlib>=3.7.0

# Testing
pytest>=7.0
pytest-cov>=4.0
